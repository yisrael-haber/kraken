/* wolfSSL and wolfSSH share wolfSSL's types, so they are translated together.
 * Their configuration, including the system headers it needs, is
 * vendor/wolfssl/kraken/user_settings.h. */
#include <wolfssl/ssl.h>
#include "kraken/ssh_shim.h"
